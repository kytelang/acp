[package]
name = "acp-cli"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Agent Control Plane: acp CLI - init, verify, export, policy-compile, policy-test."

[dependencies]
acp-core = { path = "../acp-core" }
acp-policy = { path = "../acp-policy" }
acp-nativecompile = { path = "../acp-nativecompile" }
acp-ledger = { path = "../acp-ledger" }
acp-approvals = { path = "../acp-approvals" }
acp-registry = { path = "../acp-registry" }
hex.workspace = true
serde_json.workspace = true

[dev-dependencies]
acp-ledger = { path = "../acp-ledger" }
acp-core = { path = "../acp-core" }
serde_json.workspace = true
