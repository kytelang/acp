[package]
name = "acp-mtls"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Agent Control Plane: mutual TLS config for proxy<->server (rustls, client-cert required)."

[dependencies]
rustls = "0.23"
rustls-pemfile = "2"

[dev-dependencies]
rustls = "0.23"
