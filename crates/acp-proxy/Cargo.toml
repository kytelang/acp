[package]
name = "acp-proxy"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Agent Control Plane: transparent MCP interception proxy (stdio + HTTP)."

[dependencies]
acp-core = { path = "../acp-core" }
acp-jsonrpc = { path = "../acp-jsonrpc" }
# to wire: tokio, hyper (M1/M5)
