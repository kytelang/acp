[package]
name = "acp-gateway"
version = "0.0.0"
edition = "2021"
description = "Agent Control Plane: LLM gateway PEP governing direct model API calls (phase C)."

[dependencies]
acp-hsm = { path = "../acp-hsm" }
acp-obs = { path = "../acp-obs" }
tracing.workspace = true
acp-core = { path = "../acp-core" }
acp-policy = { path = "../acp-policy" }
acp-auth = { path = "../acp-auth" }
acp-ledger = { path = "../acp-ledger" }
acp-pgstate = { path = "../acp-pgstate" }
axum = { workspace = true }
tokio = { workspace = true }
reqwest.workspace = true
serde_json.workspace = true
anyhow.workspace = true
hex.workspace = true
futures-util = { version = "0.3", default-features = false, features = ["alloc"] }
