[package]
name = "acp-gateway"
version = "0.0.0"
edition = "2021"
description = "Agent Control Plane: LLM gateway PEP governing direct model API calls (phase C)."

[dependencies]
acp-core = { path = "../acp-core" }
acp-policy = { path = "../acp-policy" }
acp-auth = { path = "../acp-auth" }
axum = { workspace = true }
tokio = { workspace = true }
reqwest.workspace = true
serde_json.workspace = true
anyhow.workspace = true
