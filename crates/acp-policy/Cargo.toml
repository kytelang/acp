[package]
name = "acp-policy"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Policy authoring: the YAML DSL and its compiler to Cedar (decision D2)."

[dependencies]
acp-core = { path = "../acp-core" }
serde.workspace = true
serde_yaml.workspace = true

[dev-dependencies]
serde_json.workspace = true
