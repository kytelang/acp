[package]
name = "acp-policy"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Policy authoring: the YAML DSL and its compiler to Cedar, plus the Cedar evaluation engine (decisions D2/D9)."

[dependencies]
acp-core = { path = "../acp-core" }
serde.workspace = true
serde_yaml.workspace = true
serde_json.workspace = true
cedar-policy = "4"

[dev-dependencies]
serde_json.workspace = true
