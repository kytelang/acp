[package]
name = "acp-auth"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Agent Control Plane: OIDC/JWT auth (Azure Entra ID model) + RBAC. Mock IdP for tests."

[dependencies]
acp-core = { path = "../acp-core" }
serde.workspace = true
serde_json.workspace = true
base64.workspace = true
ring = "0.17"
hex.workspace = true

[dev-dependencies]
serde_json.workspace = true
