[package]
name = "acp-pgstate"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Agent Control Plane: Postgres-backed shared state (rate-limit budgets and tool-integrity pins) for multi-replica PEPs."

[dependencies]
acp-core = { path = "../acp-core" }
tokio-postgres.workspace = true
tokio.workspace = true

[dev-dependencies]
tokio.workspace = true
