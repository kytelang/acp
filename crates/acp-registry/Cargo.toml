[package]
name = "acp-registry"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Agent Control Plane: app + agent registration and verified identity."

[dependencies]
acp-core = { path = "../acp-core" }
serde.workspace = true
serde_json.workspace = true
hex.workspace = true
getrandom.workspace = true

[dev-dependencies]
serde_json.workspace = true
