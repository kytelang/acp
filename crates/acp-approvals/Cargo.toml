[package]
name = "acp-approvals"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Agent Control Plane: step-up approval store with D8 integrity (single-use, caller-bound, canonical-bound, TTL)."

[dependencies]
rusqlite.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
serde_json.workspace = true
